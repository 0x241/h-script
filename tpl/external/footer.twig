	<footer class="bg-white dark:bg-[#0A0A0A] border-t border-gray-200 dark:border-gray-800 mt-auto">
		<div class="max-w-7xl mx-auto px-6 py-16">
			<div class="flex flex-col md:flex-row justify-between items-center gap-6 mb-12">
				{% include "external/logo.twig" %}
				<div class="flex flex-wrap justify-center gap-x-6 gap-y-3 text-sm font-medium text-textDim dark:text-gray-400">
					<a href="https://github.com/0x241/h-script" target="_blank" rel="noopener noreferrer" class="hover:text-brand dark:hover:text-white transition-colors">{{ _t({"key": 'site.documentation', "default": 'Документация'}) }}</a>
					<a href="{{ _link('faq', false, 0) }}" class="hover:text-brand dark:hover:text-white transition-colors">{{ _t({"key": 'nav.faq', "default": 'FAQ'}) }}</a>
					<a href="{{ _link('news', false, 0) }}" class="hover:text-brand dark:hover:text-white transition-colors">{{ _t({"key": 'nav.news', "default": 'Новости'}) }}</a>
					<a href="{{ _link('review', false, 0) }}" class="hover:text-brand dark:hover:text-white transition-colors">{{ _t({"key": 'nav.reviews', "default": 'Отзывы'}) }}</a>
					<a href="{{ _link('udp/rules', false, 0) }}" class="hover:text-brand dark:hover:text-white transition-colors">{{ _t({"key": 'nav.rules', "default": 'Правила'}) }}</a>
				</div>
			</div>
			<div class="flex flex-col md:flex-row justify-between items-center gap-4 pt-8 border-t border-gray-100 dark:border-gray-900">
				<div class="text-sm font-medium text-gray-500 dark:text-gray-600">
					{% include "external/product-copyright.twig" %}
				</div>
				<a href="{{ fullURL() }}#content" hx-boost="false" class="flex items-center gap-2 px-4 py-2 text-sm font-bold text-brand dark:text-gray-300 hover:text-blue-500 dark:hover:text-white transition-colors bg-gray-50 dark:bg-[#151515] rounded-full border border-gray-200 dark:border-gray-800">
					{{ _t({"key": 'site.back_to_top', "default": 'Наверх'}) }} <i class="fa-solid fa-arrow-up" aria-hidden="true"></i>
				</a>
			</div>
		</div>
	</footer>
</body>
</html>
